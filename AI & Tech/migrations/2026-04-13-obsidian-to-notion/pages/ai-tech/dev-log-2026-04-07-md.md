# Dev Log 2026 04 07

*Source: AI & Tech/dev-log-2026-04-07.md*

# Dev Log — 2026-04-07

## Summary
Heavy infrastructure day. The jpate supersystem went from partially wired to fully operational. All 16 system checks passing by end of day.

---

## What Got Done

### Obsidian Local REST API MCP — Built from scratch
- Existing npm packages (`mcp-obsidian`, `obsidian-mcp`) are filesystem-based — neither uses the REST API
- Wrote a custom MCP server: `jpate/tools/obsidian-mcp/index.js`
- Stack: `@modelcontextprotocol/sdk`, Obsidian Local REST API (port 27124), `NODE_TLS_REJECT_UNAUTHORIZED=0` for self-signed localhost cert
- 7 tools exposed: `read_note`, `write_note`, `append_note`, `search`, `list`, `delete_note`, `active_note`
- Wired into `~/.claude.json` — loads automatically on every Claude Code session
- Tested: MCP handshake, tools/list, live vault list all passing

### MCP Path Fix
- Root cause of prior Obsidian MCP failures: `~/.claude.json` pointed to `Desktop/jpate/tools/obsidian-mcp/index.js` (wrong)
- Correct path: `JPATE/tools/obsidian-mcp/index.js`
- Fixed in place. Tools now appear in deferred tool list every session.

### Full System Diagnosis — 16/16 Checks Passing
| Check | Result |
|-------|--------|
| SessionStart hook | Fires — reads Claude-Brain/ + Notion context |
| End-of-session trap (~/.bashrc) | Present |
| End-of-session trap (~/.bash_profile) | Added (login shells now covered) |
| Git remote | jacksonpate/jpate |
| Recent commits | Today (2026-04-07) |
| Notion MCP | Connected |
| Google Calendar MCP | Connected, America/Chicago |
| Gmail MCP | Connected, jacksonlukepate@gmail.com |
| Obsidian MCP | Live, 7 tools active |
| Vault count | ~70-72 .md files |
| ANI.md | All 4 identities: ALEX, ANI, NOVA, ORACLE |

### Clarified: Project_P is NOT a git repo — intentional
- Vault = brain, not code. `jpate` = code repo. Separate by design.
- Do not flag as issue in future system checks.

### Known Issue Logged
- `NOTION_TOKEN` not set in `.env` — Notion context returns error in session-start hook
- Session still loads on Obsidian context only; Notion section shows `[Notion unreachable]`
- Fix: add `NOTION_TOKEN` to `jpate/.env`

---

## Files Produced / Modified
- `jpate/tools/obsidian-mcp/index.js` — custom MCP server
- `jpate/tools/obsidian-mcp/package.json` — dependencies
- `~/.claude.json` — Obsidian MCP entry added + path corrected
- `~/.bash_profile` — session-end trap added
- `Memory/project_vault-repo-separation.md` — new memory file
- `agents/JOURNAL.md` — multiple entries appended
