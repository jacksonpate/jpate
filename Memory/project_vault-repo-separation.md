---
name: Project_P and jpate are intentionally separate
description: Project_P is not a git repo on purpose — vault and code repo are deliberately kept separate
type: project
---

Project_P (`C:\Users\jacks\Desktop\Project_P`) is intentionally NOT a git repository.

**Why:** The vault is the brain — a living memory and knowledge system. `jpate` (`C:\Users\jacks\Desktop\jpate`) is the code repo. They serve different purposes and are kept separate by design.

**How to apply:** Do not flag "Project_P is not a git repo" as an issue in system diagnostics or health checks. Do not suggest initializing it as a git repo. The separation is intentional and should be respected.
